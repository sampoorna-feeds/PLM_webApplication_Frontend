import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // For static hosting, uncomment the line below:
  // output: 'export',
  // Note: When using 'export', API routes won't work (which is fine since we removed them)
};

export default nextConfig;
